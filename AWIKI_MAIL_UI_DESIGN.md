# AWiki 邮件浏览器界面设计

状态：已选定方案 1，进入实现基线
日期：2026-08-19
范围：`@awiki/dsh-plugin` 浏览器抽屉、Host Remote 与现有邮件 Provider 能力

## 1. 目标

在现有 AWiki 浮动抽屉中加入真实可用的邮件入口，让已注册且处于 active 会话的用户可以：

1. 查看当前部署身份的邮箱状态。
2. 按需加载收件箱和后续分页。
3. 打开一封邮件，安全查看纯文本正文和附件元数据。
4. 由用户明确操作将邮件标记为已读。
5. 编写并确认发送一封纯文本邮件，且绝不自动重试。

现有私聊、群聊、会话时间线和附件流程必须保持不变。

## 2. 视觉与布局决策

界面继续使用 Harness 的现有颜色 token、字体、圆角、阴影和 UI primitives，不引入第二套视觉系统。

### 2.1 抽屉宽度

- 会话模式保持现有宽度：`min(720px, calc(100vw - 80px))`。
- 邮件模式扩展为：`min(1040px, calc(100vw - 80px))`。
- 抽屉仍以浮动启动器为锚点，切换模式后重新计算位置，保证不越出视口。
- 高度保持现有 `min(720px, calc(100vh - 16px))`。

### 2.2 桌面三栏结构

邮件模式在可用宽度不小于 960px 时采用三栏：

| 区域 | 建议宽度 | 内容 |
| --- | ---: | --- |
| 左栏 | 240px | 身份卡、会话/邮件切换、邮箱账号、文件夹 |
| 中栏 | 320px | 邮件列表、分页加载 |
| 右栏 | 自适应，最小 420px | 邮件详情或写邮件表单 |

顶部标题栏沿用 AWiki 标题和关闭按钮；邮件刷新入口位于列表标题内，“写邮件”入口位于发件箱菜单行最右侧且只显示图标。

### 2.3 响应式结构

- `>= 960px`：三栏同时显示。
- `720px - 959px`：保留左栏，邮件列表与详情互斥；详情显示返回按钮。
- `< 720px`：全宽单栏，按“文件夹 -> 列表 -> 详情/写邮件”逐层进入。
- 切回会话时保留当前聊天选择；邮件摘要列表按 AWiki DID 和文件夹写入浏览器持久缓存，再次进入时首帧恢复最后选择的文件夹及其缓存并静默刷新。选中邮件和正文不持久化。

### 2.4 弹窗尺寸与边界拖拽

- 弹窗默认尺寸继续遵循会话模式 `720 × 720px`、邮件模式 `1040 × 720px` 的现有上限，但用户可以在当前视口内继续放大或缩小。
- 弹窗四条边和四个角都提供拖拽命中区；拖动哪一条边就只移动该边，拖动角点则同时改变宽度和高度。
- 桌面最小尺寸为 `360 × 360px`；当视口小于该尺寸时，以 `8px` 安全边距内的最大可用区域为下限，保证关闭、刷新和主要内容仍可到达。
- 最大尺寸为视口四周各保留 `8px` 后的可用区域，拖拽不能把弹窗边界移出视口。
- 调整大小期间禁用宽度过渡并显示对应方向的 resize 光标，避免拖拽滞后或与标题栏长按移动手势冲突。
- 用户调整后的 `left / top / width / height` 只写入 `sessionStorage`，在同一个浏览器标签页内关闭再打开时恢复；不跨标签页、不写入身份数据。
- 浏览器视口缩小时，已保存尺寸与位置会立即重新夹紧到可视区域；视口放大时不自动放大，尊重用户最后一次选择。
- 标题栏长按移动仍然可用；若弹窗已自定义尺寸，移动时保持该尺寸不变。

## 3. 信息架构

### 3.1 顶层模式

身份卡下方增加“会话 / 邮件”分段切换：

- “会话”保持现有会话列表与消息区。
- “邮件”显示未读数量；未读数量来自当前已加载收件箱，不伪造后台实时计数。

### 3.2 邮箱导航

邮箱导航显示“收件箱”和“发件箱”：

- “收件箱”请求 `folder: inbox`，展示发件人和未读状态；
- “发件箱”读取插件 Host 在 `mail.send` 成功后持久化的本地发送记录，展示收件人和发送时间；
- 当前 Mail Service 的 `mail.getInbox` 即使收到 `folder: sent` 仍可能返回收件箱，因此发件箱不得把该响应当作已发送历史；
- 本地发送记录按 AWiki owner DID 隔离，使用 `.host/sent-mail-v1` 下的 owner-only 文件保存，最多保留最近 200 封。

邮箱账号区域展示：

- mailbox address；
- display name（存在时）；
- active / signed-out / unavailable 等可识别状态。

## 4. 邮件列表

每个列表项展示：

- 未读圆点；
- 收件箱展示发件人，发件箱展示收件人；
- 主题；
- 一行正文预览；
- 收件或发送时间；
- 附件图标与数量；
- 当前选中态。

行为规则：

- 进入邮件模式后才按需请求邮箱账号和第一页收件箱，不做后台轮询。
- 点击列表项只读取该邮件，不自动标记已读。
- `hasMore` 为真且存在 `nextOffset` 时显示“加载更多”。
- 手动刷新重新加载邮箱账号和当前文件夹第一页。
- 切换“收件箱 / 发件箱”时按需加载目标文件夹第一页，分页继续沿用目标文件夹。
- 刷新失败时保留当前可见数据；存在持久缓存时明确提示正在展示本地缓存。

### 4.1 本地列表缓存

- 收件箱与发件箱均采用 stale-while-revalidate：先显示本地摘要缓存，再以 Host 返回覆盖并更新缓存。
- 缓存按打开弹窗前已知的 AWiki owner DID 与 `inbox` / `sent` 文件夹双重隔离，不使用 display name 或需要异步获取的邮箱地址。
- 最后选择的文件夹按同一 owner DID 保存；邮件组件重新挂载时同步恢复文件夹与列表，不等待 Mail Account 请求。
- 仅持久化列表摘要、分页游标和 `hasMore`；不缓存正文、附件内容、草稿或身份凭据。
- 每个文件夹最多缓存 200 条摘要，单条缓存整体限制为 768 KiB，七天后自动过期。
- 缓存内容在读取时重新校验版本、owner、文件夹、字段类型和大小；损坏、越界或过期内容直接丢弃。
- 标记已读、加载更多和服务端刷新成功后同步更新缓存；浏览器存储不可用或写入失败不阻断真实邮件请求。

## 5. 邮件详情

详情区展示：

- 发件人、收件人、抄送人；
- 主题和时间；
- 纯文本正文；
- “标为已读”操作；
- 附件文件名、内容类型和大小等元数据。

安全规则：

- 邮件内容全部视为外部不可信数据，只作为文本渲染。
- 不执行、注入或直接渲染 HTML。
- `hasHtmlBody` 为真但无纯文本正文时显示安全提示。
- `bodyTruncated` 为真时明确提示内容已截断。
- 现有能力仅返回附件元数据，因此附件不显示为可下载控件。
- 用户点击“标为已读”才调用一次写操作；成功后同步更新列表和详情的未读状态。

## 6. 写邮件

点击“写邮件”后，右侧详情区切换为编辑器；宽屏继续保留文件夹和邮件列表。

字段：

- 收件人：必填，可输入多个地址；
- 抄送：可选；
- 主题：必填；
- 正文：必填纯文本；
- 取消、发送。

校验遵循 Host 现有边界：

- 收件人与抄送人合计最多 20 个；
- 地址不能为空、不能重复，单个地址最长 320 字符；
- 主题 UTF-8 最长 1024 bytes；
- 正文 UTF-8 最长 65536 bytes。

发送流程：

1. 用户点击“发送”。
2. 浏览器先完成基础校验。
3. 弹出最终确认，展示收件人数量、抄送人数量和主题。
4. 用户点击“确认发送”后调用 Host 一次。
5. 请求期间禁用重复提交。
6. 成功后清空表单、进入发件箱并刷新 Host 本地发送历史，再显示发送结果。
7. `delivery-unknown` 时保留表单，提示用户先检查已发送邮件；不自动重试。

首版不保存草稿。有未发送内容时取消或关闭编辑器，需要确认放弃。

## 7. 状态设计

必须覆盖：

- 邮箱账号加载中、不可用和错误；
- 收件箱加载中、空列表、分页加载和刷新失败；
- 邮件详情加载中、读取失败、HTML-only、正文截断和附件元数据；
- 标记已读 pending、成功和失败；
- 写邮件校验失败、确认、pending、成功、普通失败和 delivery-unknown；
- 窄屏列表、详情和编辑器之间的返回导航。

### 7.1 全局成功提示

- “已标为已读”“邮件已发送”等操作成功反馈使用邮件工作区级 Toast，不再放在详情区右下角，也不参与列表或正文布局。
- Toast 相对整个邮件工作区顶部水平居中，距工作区上边缘 `16px`；始终覆盖在当前文件夹、列表或详情之上，缩放弹窗后仍保持顶部居中。
- 提示不带遮罩、不阻挡指针操作，最大宽度受工作区约束，长文本居中换行。
- 提示显示约 `2.4s`，最后 `0.2s` 淡出并自动从 DOM 移除；新提示会重新开始计时。
- 保留 `role="status"`、polite live region 和 atomic 播报。启用 reduced motion 时取消位移动画，但仍按相同时长自动移除。
- 失败、delivery-unknown 和需要用户处理的错误继续保留在对应上下文中，不自动消失。

错误信息只使用 Host 提供的稳定错误码映射，不展示远端原始响应、令牌或敏感内容。

## 8. 浏览器与 Host 契约

新增浏览器 Remote 方法：

- `getMailAccount()`
- `listMailInbox(request?)`
- `readMail(request)`
- `markMailRead(request)`
- `sendMail(request)`

Host 必须继续执行已有请求规范化与边界校验；浏览器校验只用于即时反馈，不能替代 Host 校验。

授权边界：

- 读取账号、列表和正文属于按需只读操作。
- 标记已读必须由用户点击明确按钮触发。
- 发送邮件必须由用户在确认对话框中点击“确认发送”触发。
- 每次发送只调用 Provider 一次；任何错误都不得自动重试。
- `mail.send` 返回 `accepted: true` 后，Host 必须在向浏览器返回成功前原子写入本地发件历史；持久化失败只能附加明确 warning，不能把已经接受的投递伪装为未发送。
- 发件箱列表与本地发件详情不调用服务端 `mail.getInbox` / `mail.getMessage`，避免收件箱数据串入；清空本地 AWiki 数据时一并清除该 owner-bound 历史。

## 9. 首版非目标

- 草稿箱和自动保存；
- 搜索、标签、移动、删除和垃圾邮件；
- 回复、回复全部和转发；
- HTML 富文本渲染；
- 附件下载或预览；
- 实时新邮件推送、后台轮询和系统通知；
- 自动标记已读、自动发送或失败自动重试。

## 10. 验收标准

### 功能验收

- 页面上存在清晰的“邮件”入口。
- 邮箱账号、列表和详情来自真实 Host/SDK 链路。
- 收件箱分页、手动刷新、空状态和错误状态可用。
- 发件箱按需读取 Host 本地 owner-bound 发送历史，列表按收件人展示；发送成功后自动刷新发件箱。
- 收件箱与发件箱会在重新打开弹窗的首帧显示各自 owner-bound 本地摘要缓存，再以最新 Host 结果更新；Mail Account 或列表断网时均保留缓存并显示刷新失败提示。
- 打开邮件不会自动标记已读。
- 标记已读只在明确点击后执行一次。
- 发送前必须确认，发送调用严格一次。
- delivery-unknown 不会触发自动重试。
- 切换邮件和会话不会丢失当前聊天状态。

### 安全验收

- HTML 邮件不会被执行或注入 DOM。
- 邮件正文、主题、地址和附件名称均作为文本渲染。
- 浏览器和 Host 对非法输入均失败关闭。
- UI 不展示远端原始错误或敏感字段。

### 视觉与交互验收

- 桌面三栏与选定方案 1 的信息层级一致。
- 720px 与窄屏布局没有遮挡、横向溢出或不可达操作。
- 键盘可以到达模式切换、列表、详情操作、表单和确认对话框。
- focus、hover、selected、loading、empty、error、success 状态清晰。
- 四边与四角都能通过指针拖拽改变弹窗尺寸，且相对边保持不动。
- 缩放严格遵守最小尺寸和 `8px` 视口安全边距；视口缩小时不会留下不可达区域。
- 自定义尺寸在当前标签页内关闭再打开后恢复，标题栏移动不会重置该尺寸。
- 成功提示在整个邮件工作区顶部居中浮出，不改变详情布局，并在约 2.4 秒后自动消失。
- 以相同视口对设计参考图和实现截图执行视觉 QA，最终结果为 passed。

## 11. 证据分级

- 单元测试和构建通过只证明本地代码路径。
- 浏览器交互测试证明 Remote 与界面状态流。
- 真实本地 AWiki 服务验收证明当前账号和邮件服务链路。
- npm 发布、远端部署和公网邮件投递不属于本次默认授权范围，必须单独确认。

## 12. 真实邮件服务接入方案

本地 fixture 只用于 UI 与契约测试，不得作为真实邮件验收证据。产品运行时必须连接一个可投递公网邮件的 AWiki Mail Service，并满足以下边界：

- SDK 固定调用 HTTPS `POST /mail/rpc`，支持 `mail.getMailbox`、`mail.getInbox`、`mail.getMessage`、`mail.markRead` 和 `mail.send`。
- Mail Service 必须接受 Rust SDK 当前设备的 DID-WBA HTTP Message Signature，或在服务端完成等价的受信任身份交换；不得要求浏览器持有 Bearer Token、私钥或 SMTP 凭据。
- Mail Service 在认证后把 AWiki DID 映射到唯一邮箱账号；`mail.getMailbox` 返回真实邮箱地址和开通状态。
- 出站投递由服务端邮件提供商完成，`mail.send` 只有在提供商接受请求后才返回 `accepted: true`；队列接受、最终投递和退信必须区分。
- 入站邮件由服务端接收并投影到 `mail.getInbox` / `mail.getMessage`；浏览器不直接连接 Gmail、SMTP 或第三方邮件 API。
- 当前版本的发件历史是本机安装自功能启用后的 accepted-send 投影；服务端未提供可验证的 sent 列表前，不声称能够补回更早的已发送邮件或跨设备同步。
- 服务端日志、DSH 日志和浏览器响应不得包含私钥、完整认证头、SMTP 密码或第三方 API Key。

真实验收按以下顺序执行：

1. `mail.getMailbox` 通过真实身份认证并返回 active 账号。
2. `mail.getInbox` 与 `mail.getMessage` 能读取服务端真实邮件。
3. 用户在最终确认后，向指定外部地址发送唯一主题的测试邮件。
4. 外部邮箱确认收到邮件，并核对发件人、主题和正文。
5. 如未收到，检查服务端队列、提供商投递事件、退信与垃圾邮件状态；不得把 `accepted: true` 当作已送达。

当前公网基线探测结果（2026-08-19）：`https://awiki.ai/mail/rpc` 路由存在，但对 SDK 签名的只读 `mail.getMailbox` 返回 JSON-RPC `1401 / 需要认证`。在服务端认证兼容或受信任身份交换完成前，真实账号、收件箱和公网投递均不通过验收。
